<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.autcion.auction_back.UsersPage.dao.UsersMapper">

    <!-- loginRow --> 

    <select id="loginRow" parameterType="com.autcion.auction_back.UsersPage.domain.LoginDto" resultType="Integer">
        SELECT
            COUNT(*)
        FROM
            users
        WHERE
            login_id = #{loginId}
            AND password = #{password}
    </select>

    <!-- registerRow --> 

    <insert id="registerRow" parameterType="com.autcion.auction_back.UsersPage.domain.UserDataDto">
        INSERT INTO users (login_id, password, name, nickname, email, phone, address, created_at)
        VALUES (#{loginId}, #{password}, #{name}, #{nickname}, #{email}, #{phone}, #{address}, NOW())
    </insert>

    <!-- profile --> 

    <select id="profileRow" parameterType="String" resultType="com.autcion.auction_back.UsersPage.dao.ProfileDao">
        SELECT * FROM users WHERE login_id = #{value}
    </select>

    <!-- updateProfileRow --> 

    <update id="updateProfileRow" parameterType="com.autcion.auction_back.UsersPage.domain.UserDataDto">
        UPDATE users 
        SET name = #{name}, nickname = #{nickname}, email = #{email}, phone = #{phone}, address = #{address}
        WHERE nickname = #{nickname}
    </update>

    <!-- recoverId --> 

    <select id="recoverIdRow" parameterType="com.autcion.auction_back.UsersPage.domain.UserDataDto" resultType="String">
        SELECT login_id FROM users WHERE name = #{name} AND phone = #{phone}
    </select>    

    <!-- recoverPassword --> 

    <select id="recoverPasswordRow" parameterType="com.autcion.auction_back.UsersPage.domain.UserDataDto" resultType="Integer">
        SELECT count(*) FROM users 
        WHERE login_id = #{loginId} 
        AND name = #{name} 
        AND phone = #{phone}
    </select>    

    <!-- updatePassword --> 

    <update id="updatePasswordRow" parameterType="com.autcion.auction_back.UsersPage.domain.UserDataDto">
        UPDATE users SET password = #{password} WHERE login_id = #{loginId}
    </update>

    <!-- deleteAccountRow --> 

    <update id="deleteAccountRow">
        UPDATE users SET is_deleted = 1 WHERE login_id = #{loginId}
    </update>

    <!-- checkMileageRow --> 

    <select id="checkMileageRow" parameterType="String" resultType="com.autcion.auction_back.UsersPage.domain.MileageDto">
        SELECT 
            m.user_id,
            m.amount,
            m.created_at,
            m.updated_at,
            (SELECT SUM(amount) FROM mileage WHERE user_id = #{user_id}) AS total_mileage
        FROM mileage m
        WHERE m.user_id = #{user_id}
        ORDER BY m.created_at DESC
    </select>

    <!-- myBidsRow --> 

    <select id="myBidsRow" parameterType="String" resultType="com.autcion.auction_back.UsersPage.domain.AuctionBidsDto">
        SELECT 
            item_id,
            bidder_id,
            bidder_nickname,
            bid_price,
            bid_time
        FROM bids
        WHERE bidder_id = #{user_id}
        ORDER BY bid_time DESC
    </select>


    <!-- inquiriesByStatusRow --> 

    <select id="inquiriesByStatusRow" parameterType="String" resultType="com.autcion.auction_back.UsersPage.domain.InquiryDto">
        SELECT * FROM inquiries WHERE user_id = #{user_id} AND response_status = #{status}
    </select>

    <!-- inquiriesRow -->

    <select id="inquiriesRow" parameterType="String" resultType="com.autcion.auction_back.UsersPage.domain.InquiryDto">
        SELECT * FROM inquiries WHERE user_id = #{user_id}
    </select>
    
    </mapper>